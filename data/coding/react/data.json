[
  {
    "id": 4,
    "question": "How do you build a counter app in React with increment, decrement, and reset controls?",
    "explanation": "The count value is stored with useState, starting at 0. handleIncrement and handleDecrement update it using the functional form of setCount (prevCount => ...) so the update always works off the latest state value rather than a possibly stale count from a previous render. handleReset simply sets count back to 0. Each button calls its respective handler through onClick, and the current count is displayed directly in the JSX. Since this is a single self-contained page component, all the code lives in one file.",
    "files": [
      {
        "fileName": "app/page.tsx",
        "code": "\"use client\";\n\nimport { useState } from \"react\";\nimport { Minus, Plus, RotateCcw } from \"lucide-react\";\n\nconst Page = () => {\n  const [count, setCount] = useState(0);\n\n  const handleIncrement = () => {\n    setCount((prevCount) => prevCount + 1);\n  };\n\n  const handleDecrement = () => {\n    setCount((prevCount) => prevCount - 1);\n  };\n\n  const handleReset = () => {\n    setCount(0);\n  };\n\n  return (\n    <main className=\"min-h-screen bg-slate-50 flex items-center justify-center p-5\">\n      <section className=\"w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 text-center shadow-xl shadow-slate-200/60\">\n        {/* Header */}\n        <p className=\"text-sm font-semibold uppercase tracking-[0.2em] text-cyan-600\">\n          Counter App\n        </p>\n\n        {/* Count Display */}\n        <div className=\"my-8 rounded-2xl border border-cyan-100 bg-cyan-50 py-8\">\n          <p className=\"text-sm font-medium text-slate-500\">Current Count</p>\n\n          <h2 className=\"mt-2 text-6xl font-bold text-slate-800\">{count}</h2>\n        </div>\n\n        {/* Controls */}\n        <div className=\"flex items-center justify-center gap-4\">\n          <button\n            onClick={handleDecrement}\n            aria-label=\"Decrement\"\n            className=\"flex size-14 items-center justify-center rounded-full border border-slate-200 bg-white text-slate-700 shadow-sm transition-all duration-200 hover:scale-105 hover:bg-slate-100 active:scale-95\"\n          >\n            <Minus className=\"size-6\" />\n          </button>\n\n          <button\n            onClick={handleReset}\n            aria-label=\"Reset\"\n            className=\"flex size-14 items-center justify-center rounded-full bg-cyan-500 text-white shadow-lg shadow-cyan-500/25 transition-all duration-200 hover:scale-105 hover:bg-cyan-600 active:scale-95\"\n          >\n            <RotateCcw className=\"size-5\" />\n          </button>\n\n          <button\n            onClick={handleIncrement}\n            aria-label=\"Increment\"\n            className=\"flex size-14 items-center justify-center rounded-full border border-slate-200 bg-white text-slate-700 shadow-sm transition-all duration-200 hover:scale-105 hover:bg-slate-100 active:scale-95\"\n          >\n            <Plus className=\"size-6\" />\n          </button>\n        </div>\n      </section>\n    </main>\n  );\n};\n\nexport default Page;"
      }
    ]
  },
  {
    "id": 2,
    "question": "How do you build a stopwatch component in React with start, pause, and reset controls?",
    "explanation": "The time in seconds is stored with useState, and a useEffect hook manages the interval: whenever isRunning becomes true, it starts a setInterval that increments time every second, and the effect's cleanup function clears that interval whenever isRunning changes or the component unmounts. This prevents multiple intervals from stacking up. The raw seconds are then converted into a minutes:seconds display using Math.floor and the modulo operator, with padStart used to keep two digits. Start/pause toggles isRunning, while reset stops the timer and sets time back to zero. Since this is a single self-contained page component, all the code lives in one file.",
    "files": [
      {
        "fileName": "app/page.tsx",
        "code": "\"use client\";\n\nimport { useEffect, useState } from \"react\";\nimport { Pause, Play, TimerReset } from \"lucide-react\";\n\nconst Page = () => {\n  const [time, setTime] = useState<number>(0);\n  const [isRunning, setIsRunning] = useState<boolean>(false);\n\n  useEffect(() => {\n    let interval: ReturnType<typeof setInterval> | undefined;\n\n    if (isRunning) {\n      interval = setInterval(() => {\n        setTime((prevTime) => prevTime + 1);\n      }, 1000);\n    }\n\n    return () => {\n      clearInterval(interval);\n    };\n  }, [isRunning]);\n\n  const handleStart = () => {\n    setIsRunning(true);\n  };\n\n  const handleStop = () => {\n    setIsRunning(false);\n  };\n\n  const handleReset = () => {\n    setIsRunning(false);\n    setTime(0);\n  };\n\n  const minutes = Math.floor(time / 60);\n  const seconds = time % 60;\n\n  const formattedTime = `${String(minutes).padStart(\n    2,\n    \"0\",\n  )}:${String(seconds).padStart(2, \"0\")}`;\n\n  return (\n    <main className=\"min-h-screen bg-slate-50 flex items-center justify-center p-6\">\n      <section className=\"w-full max-w-sm rounded-3xl border border-slate-200 bg-white p-8 shadow-xl shadow-slate-200/60\">\n        {/* Header */}\n        <div className=\"mb-8 text-center\">\n          <p className=\"mb-2 text-sm font-semibold tracking-[0.2em] text-cyan-600 uppercase\">\n            Stopwatch\n          </p>\n\n          <p className=\"text-sm text-slate-500\">\n            Track your time with precision\n          </p>\n        </div>\n\n        {/* Timer */}\n        <div className=\"mb-8 flex aspect-square w-full items-center justify-center rounded-full border-10 border-cyan-100 bg-cyan-50\">\n          <h1 className=\"font-mono text-5xl font-bold tracking-tight text-slate-800 sm:text-6xl\">\n            {formattedTime}\n          </h1>\n        </div>\n\n        {/* Controls */}\n        <div className=\"flex justify-center gap-4\">\n          <button\n            onClick={isRunning ? handleStop : handleStart}\n            aria-label={isRunning ? \"Pause stopwatch\" : \"Start stopwatch\"}\n            className=\"flex size-14 items-center justify-center rounded-full bg-cyan-500 text-white shadow-lg shadow-cyan-500/25 transition-all duration-200 hover:scale-105 hover:bg-cyan-600 active:scale-95\"\n          >\n            {isRunning ? (\n              <Pause className=\"size-6 fill-current\" />\n            ) : (\n              <Play className=\"size-6 fill-current\" />\n            )}\n          </button>\n\n          <button\n            onClick={handleReset}\n            aria-label=\"Reset stopwatch\"\n            className=\"flex size-14 items-center justify-center rounded-full border border-slate-200 bg-white text-slate-600 shadow-sm transition-all duration-200 hover:scale-105 hover:bg-slate-100 hover:text-slate-900 active:scale-95\"\n          >\n            <TimerReset className=\"size-6\" />\n          </button>\n        </div>\n\n        {/* Status */}\n        <div className=\"mt-6 text-center\">\n          <span\n            className={`inline-flex rounded-full px-4 py-1.5 text-sm font-medium ${\n              isRunning\n                ? \"bg-green-100 text-green-700\"\n                : \"bg-slate-100 text-slate-500\"\n            }`}\n          >\n            {isRunning ? \"● Running\" : \"● Paused\"}\n          </span>\n        </div>\n      </section>\n    </main>\n  );\n};\n\nexport default Page;"
      }
    ]
  },
  {
    "id": 5,
    "question": "How do you build a tab navigation component in React that switches content based on the selected tab?",
    "explanation": "The tabs array holds each tab's id, label, and content. The active state stores the id of the currently selected tab, starting at \"home\". We derive activeTab by using find() to locate the tab object in the array whose id matches active, and its content is what gets displayed. Each tab button calls setActive with its own id on click, which updates active and causes activeTab (and therefore the rendered content) to change. The isActive check inside map() is used purely for styling, so the currently selected tab's button looks visually different from the rest. Since this is a single self-contained page component, all the code lives in one file.",
    "files": [
      {
        "fileName": "app/page.tsx",
        "code": "\"use client\";\n\nimport { useState } from \"react\";\n\nconst Page = () => {\n  const [active, setActive] = useState(\"home\");\n\n  const tabs = [\n    {\n      id: \"home\",\n      label: \"Home\",\n      content: \"This is the Home section content.\",\n    },\n    {\n      id: \"about\",\n      label: \"About\",\n      content: \"This is the About section content.\",\n    },\n    {\n      id: \"contact\",\n      label: \"Contact\",\n      content: \"This is the Contact section content.\",\n    },\n    {\n      id: \"profile\",\n      label: \"Profile\",\n      content: \"This is the Profile section content.\",\n    },\n  ];\n\n  const activeTab = tabs.find((tab) => tab.id === active);\n\n  return (\n    <main className=\"min-h-screen bg-slate-50 p-5 sm:p-10\">\n      <div className=\"mx-auto w-full max-w-2xl\">\n        {/* Header */}\n        <div className=\"mb-6\">\n          <p className=\"text-sm font-semibold uppercase tracking-[0.2em] text-blue-600\">\n            Tabs\n          </p>\n\n          <h1 className=\"mt-2 text-2xl font-bold text-slate-800\">\n            Tab Navigation\n          </h1>\n\n          <p className=\"mt-1 text-sm text-slate-500\">\n            Switch between different sections.\n          </p>\n        </div>\n\n        {/* Tabs */}\n        <div className=\"rounded-2xl border border-slate-200 bg-white p-1.5 shadow-sm\">\n          <div className=\"grid grid-cols-2 gap-1 sm:grid-cols-4\">\n            {tabs.map((tab) => {\n              const isActive = active === tab.id;\n\n              return (\n                <button\n                  key={tab.id}\n                  onClick={() => setActive(tab.id)}\n                  className={`rounded-xl px-4 py-2.5 text-sm font-medium transition-all duration-200 ${\n                    isActive\n                      ? \"bg-blue-600 text-white shadow-md shadow-blue-500/20\"\n                      : \"text-slate-500 hover:bg-slate-100 hover:text-slate-800\"\n                  }`}\n                >\n                  {tab.label}\n                </button>\n              );\n            })}\n          </div>\n        </div>\n\n        {/* Content */}\n        <div className=\"mt-4 min-h-32 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm\">\n          <div className=\"mb-3 flex items-center gap-3\">\n            <div className=\"flex size-10 items-center justify-center rounded-xl bg-blue-50 text-sm font-bold text-blue-600\">\n              {activeTab?.label.charAt(0)}\n            </div>\n\n            <div>\n              <h2 className=\"font-semibold text-slate-800\">\n                {activeTab?.label}\n              </h2>\n\n              <p className=\"text-xs text-slate-400\">Active section</p>\n            </div>\n          </div>\n\n          <p className=\"text-sm leading-6 text-slate-500\">\n            {activeTab?.content}\n          </p>\n        </div>\n      </div>\n    </main>\n  );\n};\n\nexport default Page;"
      }
    ]
  }
]
